# 会话问题索引

| 编号 | 问题 | 状态 | 最近进展 | 下一步 |
| --- | --- | --- | --- | --- |
| Q1 | 启用本次会话台账 | 已解决 | 已创建并校验基础台账结构 | 后续会话按协议维护 |
| Q2 | Elwright 自动化交互测试 | 已解决 | PR #1（含 Playwright 冒烟）已合并 main（7e222ab），CI 全绿；早期 run 失败由后续 e2e 重构与平台修复覆盖 | 无遗留 |
| Q3 | 工程质量第二档收口（tier2） | 已解决 | 合入 main（6f84109）+ 两平台 CI 修复后 7/7 全绿 | 归档待用户确认 |
| Q4 | 工作台第一阶段（Todo + 今日记录） | 已解决 | 合入 main（c0c46b1）CI 7/7 全绿 | 归档待用户确认 |
| Q5 | 双线分支合并协调（tier2/workbench vs codex） | 已完成 | PR #1 已合并 main（7e222ab），main CI 7 job 全绿；路线图/进度指针已同步（7bd0f9e） | v0.1.6 发版与任务目录归档待用户决策 |
| Q6 | 本分支下一步评估（roadmap 复查） | 已解决 | traffic-light 补做、文档同步、两轮 rebase 全部完成（详见 events Q6 各轮） | 无遗留；等合并流程推进 |
| Q7 | 归档 + v0.1.6 发版 | 已解决 | 22 目录归档；v0.1.6 发版一次全绿；但真机冒烟发现资源收藏 IPC 恒失败（见 Q8），v0.1.6 已被 v0.1.7 取代 | 真机安装直接用 v0.1.7 |
| Q8 | v0.1.6 功能冒烟（抓到发布阻断 bug） | 已完成 | bug 已修随 v0.1.7 发版；真机补点验按用户指示跳过，留档 PENDING 清单 | 清单随 v0.1.8+ 复验 |
| Q9 | 代码浏览器阶段① | 已完成 | 已合并 main 并随 v0.1.8 发版；真机点验跳过留档、任务目录归档 | 阶段③第一批已接续（见 Q12） |
| Q12 | 代码浏览器③（收藏/书签/发送到 AI + Todo 联动/终端定位） | 已完成 | 第一批随 v0.1.9 发版；第二批完成并进 main（ecda801，CI 全绿）；真机点验留档 | 真机点验待用户；随下版发版 |
| Q13 | 流程失误：绕过 PR 直推 main | 已解决 | wt-main 分支未切回导致直推；内容完整 CI 全绿，未重写历史 | 教训：提交前查分支；反引号信息用安全方式传入（Q14 一度重犯，已当场纠正） |
| Q15 | 路线图未做功能盘点 | 已解决 | 主干未完成 8 项 + 后置 2 项已列明；ROADMAP 漂移已同步（PR #5，370bb7b） | 按盘点结果排期 |
| Q16 | AI 对话阶段③④（能力协作 + 流式/取消） | 已完成 | 阶段③ PR #6 合并（4b70c27）；阶段④ 按 ADR-003 落地，PR #8 合并（84e9667，CI 全绿）；顺手修 code_browser 测试并行撞名 flake | 真机点验（流式体验/能力协作全链路）待用户；v0.1.10 待发（攒 4 个功能） |
| Q17 | 发版 v0.1.10 | 已解决 | 按用户指令发版，本地五道闸全绿（cargo 62+1+6+4+1、strict clippy 0 警告、fmt、vitest 43、build），release.yml 一次全绿（dmg 2m18s + msi 3m24s + Publish 11s），tag v0.1.10 + 完整 changelog 上 GitHub Release。ROADMAP 当前版本/里程碑/进行中三处同步更新；台账 Q16 收口 | 真机点验项延续挂起（流式/能力协作全链路 + 历史清单）；下一阶段待用户定向 |
| Q10 | CI 教训沉淀 | 已解决 | Windows IPC origin 需 cfg 区分；本地 clippy 须带 -D warnings（新 IPC 测试/命令参数两处踩坑） | 后续新增 IPC 测试照抄 terminal_ipc.rs 的 origin 处理 |
| Q18 | 代码浏览器阶段④（受控补丁编辑） | 已完成 | ADR-001 「再评估」先合（PR #9，6f5171a）；实施 PR #10 合并（90bc346，CI 7/7 全绿）。后端 core::patch 10 单测 + IPC mock runtime 3 例；前端 lib/patch.ts + PatchPreviewDialog + ChatView diff 识别 + Bridge 4 方法 + patch.test.ts 4 例；行为/架构/changelog/ADR 验证回填 + chat ③④回填（v0.1.10 漏的）。本地 87 cargo + 47 vitest + strict clippy + fmt + build 全绿 | 真机点验待用户（选中→AI 回复→应用补丁→撤销 + 敏感路径拒绝 + 写入冲突兜底） |
| Q19 | 设置中心：模型档案（多套 LLM 配置切换） | 已完成 | ADR-001 模型档案 PR #11 已合并（65a13cb）；实施 PR #12 已合并（92597f8，CI 7/7 全绿）。core::llm 5 单测 + CLI `ew config profile list/show/use/add/remove/rename` 6 子命令（key 脱敏 + 错误 exit 1）+ 5 IPC（main.rs 注册 + mock runtime 5 例）+ Bridge 5 方法 + LlmSettings.vue 档案下拉/新建/清单删除 + vitest 4 例。本地五道闸全绿（cargo 97 + strict clippy + fmt + vitest 51 + build）。文档回填 settings-center behavior/architecture/changelog/README + ROADMAP 「设置中心后续阶段」标记完成 | 真机点验（建 2 profile → 切换 → ChatView 头部变化；flat-only 旧配置兼容；删除激活档案回退 flat）待用户 |
| Q20 | 工程质量第三档（ESLint + coverage 阈值） | 已完成 | ADR-002 ESLint+coverage PR #13 已合并（f25501f）；实施 PR #14 已合并（21cfbc1，CI 7/7 全绿）。ESLint 10 flat config（`typescript-eslint` + `eslint-plugin-vue`，规则 `no-unused-vars` + `vue/no-unused-components` + `vue/no-unused-vars` + `no-explicit-any` warn；TS/Vue 下 `no-undef` 关闭；vue 风格规则 off 留 prettier 独立 ADR）；vitest coverage v8 provider（include `lib/**/*.ts` exclude `lib/bridge.ts` facade + test 文件，thresholds lines/functions/statements 70% / branches 60%）；CI frontend job 加 `npm run lint` 与 `npm run test:coverage` 两步；vitest 51 → 60（preferences 副作用新增 9 例）；实测覆盖率 95/76/100/95；TypeScript 7.0.2 → 6.0.3（typescript-eslint 8.68 显式拒绝 TS 7）。本地六道闸全绿（eslint + vitest + coverage + vite build + cargo fmt + clippy + test）。文档回填 engineering-quality README 表格 / changelog / ADR-002 链接 + ROADMAP 第三档「按需」→「已完成」 | 真机点验（故意留未使用 import → lint fail；故意在 lib/preferences.ts 加未调用函数 → coverage % 下降）已写入 verification.md【手测】段，本档不阻塞 CI；待用户点测 |
| Q21 | README「快速开始」补真机启动桌面端的命令 | 已完成 | 按用户「怎么没有启动代码的命令，就是可以直接看桌面端的」反馈；旧版「快速开始」只写 `npm run dev`（vite 浏览器预览），新人装好代码后想看桌面 app 找不到入口。已改 README.md：拆「CLI」+「桌面端（真机）」+「桌面端（浏览器预览）」三段，分别给出 `cd src-tauri && npm --prefix ../src install && ../src/node_modules/.bin/tauri dev` 与 `cd src && npm install && npm run dev`，并写明真机构建工具链前置（macOS CLT / Windows Build Tools / Linux 见 tauri 官方）、浏览器预览只覆盖「查看类」功能、其余核心功能必须真机 tauri dev；附带 `tauri build` 出 dmg/msi 与「未签名打开流程」+ `git tag` 走 CI release.yml 的提示 | 等用户复核 README 排版；后续若提 PR 则按 AGENTS.md「行为变化同步测试与 Feature 文档」追加 verification 回填，本任务为纯文档，未建 active 任务目录 |
| Q22 | 代码浏览器「选项目后看不到文件 + 预览栏比较小」 | 已完成 | 桌面 GUI 实测定案三根因：1) `treeCache` 用 Map，Vue 3 v-for 遍历 Map 得到 `[key,value]` 对儿+数字下标，`v-show="rel===''"` 恒 false → 整棵树 display:none（node+jsdom 实证：set 3 条渲染 length:2）；2) `.code-browser` 漏 `grid-column: 1/-1`（chat-view 等全宽视图都有），被 `.content` 两列网格压进 300-380px 第一列；3) App.vue 报错 toast 只在 toolbox 视图渲染，IPC 失败全静默放大 1。修复：CodeBrowserView treeCache Map→Record + style.css 补 grid-column + toast 全视图化；lint+test 60/60+build 全绿；经 vite HMR 热更到用户运行中的 tauri dev，桌面控制实测根层树行/二级展开/文件预览全通，用户本人随后续操作确认。任务目录 bugfix-2026-08-code-browser-tree-invisible（change-note+verification） | 变更未 commit，等用户复核后提交；组件挂载级测试需 @vue/test-utils（未引入），以源码注释+文档留档 |
| Q23 | 树展开子级重复 ×2 + 最大化按钮对齐 mac 原生 | 已完成 | Q23-1 树重复：Q22 修好可见性后暴露的模板结构缺陷——外层 `v-for treeCache` 把每个已缓存层级再当顶级列表渲染（与手工嵌套重复），展开 src 后 main/test 出现两份；改为 `visibleRows` computed 扁平化（按 expanded 递归、任意深度、paddingLeft 缩进），删除写死三层嵌套，★收藏统一到所有层级；真机实测 springbootDemoV1：src→main/test 各一次、main→java/resources 深度 3 无重复。Q23-2 原生绿点：点击补回全屏切换（此前 @click 空处理，早前 changelog 记载与代码不符系回归），悬停菜单加四角四分位（quarter-tl/tr/bl/br 取代 four-grid=左上角）+ 文案对齐原生（移到屏幕左侧/…）；真机实测全屏进出窗口精确恢复 71,77,1200,780。闸门 lint+test 60/60+build 全绿；任务目录 bugfix-2026-08-code-browser-tree-duplicate + enhancement-2026-08-window-layout-native；changelog 补 code-browser + app-shell | 变更未 commit；四角/三列逐项点验与全屏态菜单收敛挂 PENDING；双击标题栏缩放、Option+zoom 拒绝/后置见 plan.md |
| Q24 | 代码浏览器最近项目支持删除 | 已完成 | 全栈贯通：core `remove_recent_project`（项目+名下最近文件同删，收藏/书签保留）+ IPC `code_browser_recent_remove_project` + main.rs 注册 + mock runtime 自清理用例 + Bridge 三端 + 最近项目行 × 按钮 + toast。闸门全绿（cargo 78 核心+2 IPC、clippy 0 警告、fmt、eslint、vitest 60、build）。真机验证：× 删除行消失+持久化+V1 文件同清+toast 在代码视图显示；系统目录选择器→打开项目全链路首次真机验证通过。附带修 2 个测试基建问题：IPC 测试加 USER_STORE_LOCK（两用例并行写真实用户层竞态丢更新，本地撞出）+ 收藏/书签断言改增量式（老断言假设用户层为空，非 CI 必误报）；清掉失败运行残留的 9 条测试垃圾（全指向已删临时目录）；被删的 springbootDemo1 最近记录已恢复。任务目录 enhancement-2026-08-code-browser-recent-remove | 变更未 commit（Q21-Q24 攒一批）；系统对话框键盘导航（Cmd+Shift+G）在本机 CU 环境被拒（app 无 bundle_id），精确定位目录未验证——不影响真实用户使用 |
| Q26 | 发版 v0.1.11 | 已完成 | 流程：①版本号四处同步（Cargo.toml/tauri.conf.json/package.json/package-lock.json + Cargo.lock 随构建更新，commit 6a22153）→ 推 main → ci.yml 7/7 出制品；②下载 windows msi 制品用 `file` 提取 Revision Number = ProductCode {7638490A-6BF2-4B6C-978A-9F67D7C1320A}，同步 install.ps1（commit 6583057）；③tag v0.1.11 推送 → release.yml 全绿 → GitHub Release 验证双资产（Elwright_0.1.11_aarch64.dmg + Elwright_0.1.11_x64_en-US.msi）。本地闸门（cargo 78/vitest 60/build）先于版本号提交全绿 | 公司机装 msi 走一键脚本验证幂等升级；ROADMAP 收口随 Q27 提交 |
| Q27 | V2 剩余主干启动（AI 对话阶段④余项：长上下文） | 已完成 | 按用户「直接执行 V2 剩余主干」+ ROADMAP 顺序取 AI 对话余项。已完：摸清链路（chat_completion/commands.rs:240 与 chat_completion_stream/:887 均 system+全量 history 原样转发，无长度管理）；ADR-004 定案「core 侧字符预算滑动窗口」（fit_messages 收口两命令、默认 24000 字符可配 flat 字段 contextBudgetChars、整条丢弃不切半条、最新 user 必留、静默裁剪；拒绝 LLM 摘要/tokenizer/前端截断）写入 features/chat/decisions/ADR-004-long-context.md；任务目录 feature-2026-08-chat-long-context（plan 含实施 checklist）；ROADMAP「进行中」登记 | 实施待开工：core::chat_context 模块 + 两命令接入 + IPC 用例 + `ew config` 可见 + 文档回填；ADR 内容在最终回复中供用户否决；跨平台完善按 ADR 不单独立项、逐项走 PENDING 清单 |
| Q28 | 长上下文实施（ADR-004 落地） | 已完成 | 用户确认 ADR 后实施：core::chat_context::fit_messages（6 单测）+ assemble_chat_messages 收口 chat_completion/chat_completion_stream + contextBudgetChars 配置链（flat 字段/env/ew config 显示与 set）+ 新 IPC 测试（自起 mock LLM 服务捕获真实请求体验证裁剪：system 前置/最新 user 完整/历史≤预算/最旧裁掉）。顺手修 ew config set 按 flat 表回写抹掉 Q19 profiles 的数据丢失隐患（改经 UserConfigFile::set_flat_field，CLI 冒烟验证档案保留）。闸门全绿：cargo 84 核心+集成、clippy 0 警告、fmt、eslint、vitest 60、build。文档回填 chat behavior/architecture/changelog + ROADMAP（AI 对话条目/里程碑/进行中清空）| 长会话真机点验（需真实 LLM）留 PENDING；下一项「人与人消息会话②」待用户定向立项 |
| Q30 | 能力渐进式发布后续（成长体系透明化 ADR-001） | 已完成 | 用户在 V2 盘点后定向「先做能力渐进式发布后续」。ADR-001 定案并同轮实施：lib/growth.ts 纯函数（isUnlocked/growthSummary/newlyUnlocked，6 vitest）+ 解锁条件与进度三处可视化（List 徽标 tooltip/Detail 横幅/侧栏提示行，统一「累计使用任意能力 N 次后自动解锁（当前 T/N）」）+ 跨阈值 toast「🎉 已解锁进阶能力」+ 侧栏最近解锁进度；内置 weekly-report 升为进阶档位开箱示例（tier2+3 次，改变既有示例默认可见性已标注供否决）。浏览器预览四层实测全过（提示行/查看全部徽标/详情横幅/第3次使用触发 toast 进入核心视图，测试 localStorage 已清理）。闸门：eslint 0/vitest 66/build 全绿，cargo 零改动 | weekly-report 默认隐藏的示例策略待用户知悉；拒绝项（单能力计数/远程解锁/桌面通知）见 ADR-001 |
| Q31 | 工作台第二阶段（收藏/最近使用 + 实用工具，ADR-001） | 已完成 | 用户定向插队小项。实现：lib/capabilityRecents.ts（收藏/最近使用本机存储，去重置顶上限 8、损坏回退）+ lib/convert.ts（JSON/Base64 UTF-8 安全/时间戳⇄日期秒毫秒自动，中文报错带 cause）+ WorkbenchView 2×2 布局加「常用能力」「实用工具」两区块（点能力跳工具箱由 App openCapabilityFromWorkbench 接线，recordRecent 与使用计数同时机）。测试：vitest 81（+10）；eslint 抓 catch 抛错缺 cause 已补；e2e 抓 .wb-count 撞名已作用域化，10/10；浏览器实测新区块渲染与 JSON 格式化 | 桌面端最近使用/收藏/跳转真机点验留任务目录 |
| Q32 | i18n 基建（设置中心 ADR-002） | 已完成 | 同轮第二项。lib/i18n.ts 轻量 t() + zh-CN/en 双语字典（零依赖、键集完整性守卫测试）+ preferences.language（默认 zh-CN、切换即时生效并持久化）+ 设置中心壳层全量字符串迁移 + 「界面语言」选择器启用。增量迁移遗留：枚举选项标签、LlmSettings 内部文案、其余视图（ADR-002 第 3 条）| English 下枚举标签仍为中文（已知遗留）；其余视图迁移待增量推进 |
| Q33 | 发版 v0.1.12 | 已完成 | 沿用 v0.1.11 协议：四处版本号同步（d711791）+ ci.yml 7/7 全绿 + windows msi Revision Number 提取 ProductCode {968DA9AA-C60A-4459-9A33-D3C52864DF87} + install.ps1 + ROADMAP 三处同步（a909746）+ tag v0.1.12 推送 + release.yml 3 job 全绿（msi 3m51s + dmg 1m53s + Publish 11s）+ GitHub Release 双资产就位。携带 4 个未发版功能：AI 对话长上下文（ADR-004）+ 能力渐进式发布后续（ADR-001）+ 工作台二期（ADR-001）+ i18n 基建（ADR-002）。本地闸门全绿（cargo 84+集成 + clippy 0 + fmt + eslint + vitest 81 + coverage 95/76/100/95 + vite build + Playwright 10/10）| 真机点验延续 PENDING 清单（长会话裁剪/补丁编辑/模型档案切换/最近项目删除/v0.1.11 msi 升级安装）；下一项 V2 主干「人与人消息会话②：轻量身份/邀请与一对一消息传输」待定向 |
| Q34 | 人与人消息会话② 立项 + ADR-002 | 进行中 | 用户定向「先出 ADR 然后进行开发」。调研背景（阶段① PeopleChatView+localStorage；架构留 MessageTransport 口；core 无 people 模块）+ 出 ADR-002——自托管 WS 中继（仓库附 axum 参考实现）+ 本地 Noise_XX 静态身份（X25519 公钥 16 字符 base32 = ID）+ 6 字符短邀请码 + libsodium secretstream 流加密；弃选 WebRTC/GitHub claim/OIDC/Signal DR/公共中继（理由入 ADR）+ 用户批准 + 任务目录 `feature-2026-09-messaging-phase2/{plan,checklist,verification}.md`（6 步切分）+ ROADMAP 登记 | Step 1 协议层骨架：core::messaging_transport（snow + libsodium-sys/dryoc 评估）+ Frame 协议 + 单测（握手/密文不可破/nonce 重放/帧解析边界）+ 帧格式文档 + windows-gnu 工具链编译验证 |
