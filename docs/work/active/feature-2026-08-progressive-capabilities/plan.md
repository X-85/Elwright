# 渐进式能力成长 MVP

## 目标

为未来大量能力提供渐进式展示与透明解锁的基础，同时不隐藏现有核心能力。

## 范围

- 注册表增加能力发布档位与可选本地使用次数门槛。
- 默认展示核心能力；用户可主动查看全部能力。
- 待解锁能力可查看但不能执行。
- 使用次数和查看偏好仅保存于本机前端存储。

## 非目标

- 不上传使用习惯、不做服务端用户画像。
- 不实现远端能力下载、社区审核或自动代码执行。
- 不在本任务中改变现有三项内置示例的可见性。

## 验证

- Rust 注册表兼容性单测。
- Rust/前端全量测试与生产构建。
