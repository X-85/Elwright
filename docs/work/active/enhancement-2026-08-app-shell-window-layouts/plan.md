# 窗口布局与终端入口右移

## 目标

将终端入口移到标题栏右侧控制组，并为绿色窗口按钮增加 Codex 式悬停窗口布局面板：半屏、填充、排列、全屏和恢复窗口。

## 范围

- 标题栏右侧按“终端、设置、右栏开关”排列。
- 使用当前显示器工作区计算各类窗口布局的位置和尺寸。
- 面板只在悬停绿色按钮或面板本身时显示，移出或点击空白处关闭。
- 保留拖动、关闭、最小化和现有终端会话行为。

## 非目标

- 不实现系统级拖拽吸附预览；布局面板仅复用 Codex 的交互和视觉结构。
- 浏览器预览不执行真实窗口布局。
