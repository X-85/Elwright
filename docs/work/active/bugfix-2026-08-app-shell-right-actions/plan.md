# 应用壳右侧操作区修复（随布局回退）

> 2026-08-24 随三块区域布局一起回退，后续重构时重新设计右侧操作区。

## 问题

右侧内容面板默认隐藏时，右上方的面板、终端和设置按钮也被布局状态隐藏；浏览器预览模式还完全不显示终端入口，导致页面结构与桌面壳示意不一致。

## 处理

- 右侧内容面板的显隐只影响右下内容区，不影响右上操作区。
- 浏览器预览仍显示终端图标，但标记为桌面模式可用并禁止点击。
- Tauri 桌面模式继续保留终端展开行为。
