# Elwright Logo：方案二细化

## 目标

优化“上升动势 E”，让 E 与 AI 能量符号成为一个整体，而不是两个并排的图形。

## 设计约束

- 不使用盾牌、边框或防护型外轮廓。
- 保持黑白几何草图，先判断结构再上色。
- 闪电必须嵌入 E 的笔画或负空间。
- 只保留一个核心符号，不添加节点和装饰。

## 概念候选

- `concept-rising-e-v2a.svg`：闪电直接切入 E 的中部结构。
- `concept-rising-e-v2b.svg`：用 E 内部负空间形成闪电。
- `concept-rising-e-v2c.svg`：用 E 的右侧笔画与闪电合并成单一符号。

## 备选方案记录

- `concept-human-ai-tool.svg` 记录为备选方案：上半部分代表人，中间横线代表 AI，下半部分 L 代表工具。
- 当前不替换正式应用图标，待品牌方向确定后再制作彩色、单色和平台尺寸版本。

## 第三轮概念

- `concept-open-diamond.svg`：被打开的菱形，表达能力入口和向上空间。
- `concept-collaborative-paths.svg`：两条路径协同汇聚，表达人与 AI 的配合。
- `concept-modular-units.svg`：模块化工作单元，表达工具、知识和能力的组合。

## 第四轮定稿（参考图驱动）

用户提供手绘参考图（纯黑白粗笔、大量留白）：上横杠（右端重）+ 下横杠（左端重）+ 中间向右粗折角 + 右侧小元素。

定稿 `concept-e-forge-forward.svg`，语义映射到产品理念：

- **E 骨架**（上横杠 / 左立柱 / 下横杠）= 离线核心地基——没有 LLM 也完整可用的部分
- **中横替换为带缺口的向右折角** = 注入的前进动力——LLM 是增强不是地基
- **右侧四角星** = El（超人 Kryptonian 星/光）——品牌名的出处

正式产出（`assets/branding/`）：

- `elwright-mark.svg` — 单色主版本（白底石墨）
- `elwright-mark-dark.svg` — 深底反白版本
- `elwright-mark-color.svg` — 彩色版本（石墨结构 + 青色渐变能量）
- `app-icon.svg` → 渲染 `src-tauri/app-icon.png`（暖纸底）→ `tauri icon` 全平台尺寸

应用图标配色：暖纸底 `#f2efe8`、石墨 `#1e2833`、能量青渐变 `#2fb8a6 → #0e7f8c`。

## 第五轮定稿（单字母 E，学 ZCode 手法）

用户反馈 E+`>` 复合方案仍不够干净，提出参考 ZCode 图标：ZCode 就是一个 Z，Elwright 主要用一个 E。

分析 ZCode 图标（`/Applications/ZCode.app/Contents/Resources/icon.png`，1024×1024）：

- 纯黑满铺方底（`#000` 88% 占比），系统显示时套 squircle 圆角
- 白色字母作**负空间**挖出（黑底白字）
- 粗几何无衬线，单笔画宽度
- 字母占画面约 48% 宽 × 42% 高，居中

定稿 `concept-e-forge-forward.svg` 与正式三版本，完全照 ZCode 手法做 Elwright 的 E：

- 黑底 `#0d0d0d` + 白 E `#f5f5f5`（负空间）
- E 四笔等粗（~10.4% 画面高），中横略短于上下杠（经典 E 比例）
- 占画面 48%×42%，居中
- 彩色版：黑底白 E + 中横青渐变（`#2fb8a6→#0e7f8c`）= LLM 增强注入中横

正式产出（`assets/branding/`）：

- `elwright-mark.svg` — 黑底白 E，主版本（ZCode 式）
- `elwright-mark-light.svg` — 白底黑 E，浅色背景用
- `elwright-mark-color.svg` — 黑底白 E + 青色中横

## 第六轮定稿（单字母 E + 中横协调地变成 >）

用户在 v5（纯单字母 E）基础上要求：把 E 的中横**协调地**变成终端 `>`，而非直接替换一个 `>` 符号。重读参考图（高分辨率重解析，纠正了下臂走向的误读）确认参考图即「上横杠 + 中间 `>` 折角 + 下横杠」，`>` 为两臂在右侧汇成尖角、带细内凹缝。

定稿手法（协调 = 与横杠等粗、连在立柱上、占中横位、尖角锐利、内凹缝压到最小）：

- 保留 v5 黑底白 E（学 ZCode 负空间手法）
- 中横替换为 `>` 折角：两臂粗 104（= 横杠粗）、左端连立柱、尖端锐利于 (700,512)、内凹缝仅 12px（≈ 参考图 ~1% 缝宽）
- `>` 几乎填满中段（y402-622），小尺寸下近似实心横杠（协调），大尺寸下露出 `>` 分缝
- 彩色版 `>` 用青渐变（中横 = LLM 增强注入那一笔）

`>` path：`M368 402 L672 484 L700 512 L672 540 L368 622 L368 518 L648 512 L368 506 Z`

## 第七轮定稿（Σ 形：上横线 + 双斜折 + 下横线）

用户在第六轮基础上回到参考图原貌，提出明确要求：

- 上下两条横线都是水平直线（去掉第六轮的下横线右端上移修正）
- 中间 2 个平行四边形块要和上下两条线的左侧对齐
- 上下线和中间的 2 个平行四边形块要按参考图留缝隙，不要完全贴合

资深设计者推荐（用户采纳）：

- 平行四边形块作为反向倾斜的两个粗笔段，在右端汇聚成 Σ 折角
- 上下横线带圆角（rx=16）增加现代感；斜块保持直角（折角处自然形成 V 汇聚）
- 上下缝宽约 40-64（约 4-6% 画面）；中缝在折角处自然收口（两块重叠 18px）

定稿 `concept-e-forge-forward.svg`（以及 `app-icon.svg` / `elwright-mark.svg` 系列）：

- 黑底 `#0d0d0d` + 白 Σ `#f5f5f5`（学 ZCode 负空间）
- 上横线：`x=264..760, y=296..368`（粗 72，圆角 rx=16）
- 上斜块：左对齐 x=264，左高右低，垂直厚 72，斜率 0.083
  - 4 顶点：(264,408), (760,449), (760,521), (264,480)
- 下斜块：左对齐 x=264，反向倾斜（左低右高），垂直厚 72，斜率 0.083
  - 4 顶点：(264,544), (760,503), (760,575), (264,616)
- 下横线：`x=264..760, y=656..728`（粗 72，圆角 rx=16）
- 折角处两块重叠约 18px，形成自然 V 汇聚
- 彩色版 Σ 腹部青渐变 `#2fb8a6 → #0e7f8c`，上下横线保持白色

## 第八轮定稿（Σ 形优化：块更短更细、严格左对齐、折点 62.9%）

用户对比生成的图标与参考图，指出 v7 与参考图的差距：

- v7 块太厚（与横线同粗 72）→ 参考图块明显**比横线细**
- v7 块太长（与横线同宽 496）→ 参考图块明显**比横线短**
- v7 折点 x=760 太靠右（75% 画面）→ 参考图折点在 60-70% 区间

同时做三个候选对比（块厚 40、折点 x=644）：

- 候选 A：块左端 x=204（伸出横线 60px）—— 钩状太强（鱼钩感）
- 候选 C：块左端 x=244（伸出 20px）—— 接近参考图笔意
- 候选 B：块左端 x=264（严格 = 横线左端）—— 用户选择 B

定稿 `app-icon.svg` / `elwright-mark.svg`（v8 几何）：

- 上横线：`x=264..760, y=296..368`（粗 72，圆角 rx=16）
- 上斜块：左对齐 x=264，左高右低，垂直厚 40，斜率 0.055
  - 4 顶点：(264,412), (644,433), (644,473), (264,452)
- 下斜块：左对齐 x=264，反向倾斜（左低右高），垂直厚 40，斜率 0.055
  - 4 顶点：(264,548), (644,527), (644,567), (264,588)
- 下横线：`x=264..760, y=656..728`（粗 72，圆角 rx=16）
- 折点处两块分离约 54px（折角处有明显缝）
- 块明显比横线短（宽 380 vs 496，即 76.6% 横线宽）
- 块明显比横线细（厚 40 vs 72，即 55.6% 横线粗）
- 彩色版 Σ 腹部青渐变 `#2fb8a6 → #0e7f8c`，上下横线保持白色

## 第九轮定稿（Z 形：原图比例 + 严格左对齐 + 下横线长）

重新精读参考图（高对比度二值化 + 80字符缩放 ASCII 解析），发现之前 v3-v8 一直按"Σ 形（V 折角在右端）"做，实际原图是 **Z 形**：

- 上斜块：左上到右下（左高右低），右移
- 下斜块：右上到左下（左低右高），左移
- 两块**反向**形成 Z 字腹部，**折点中央偏右**而非右端
- 下横线明显比上横线**长**（原图下横线伸出更多）

用户要求"在原图上修改、首先把左侧对齐"——按"严格左对齐"重做：

定稿 `app-icon.svg` / `elwright-mark.svg`（v9b）：

- 上横线：`x=264..760, y=296..368`（粗 72，圆角 rx=16）
- 上斜块：左对齐 x=264，垂直厚 40，水平宽 220，斜率 0.127（左高右低）
  - 4 顶点：(264,416), (484,444), (484,484), (264,456)
- 下斜块：左对齐 x=264，垂直厚 40，水平宽 220，反向斜率 0.127（左低右高）
  - 整体下移 80（左沿 y=568/608 比上斜块的 416/456 低 80）
  - 4 顶点：(264,568), (484,540), (484,580), (264,608)
- 下横线：`x=264..844, y=664..728`（粗 64，比上横线长 84，圆角 rx=14）
- 严格左对齐：所有元素左端 x=264
- 折点中央偏右：上斜块右下 (484,484) 与下斜块右上 (484,540) 间距 56
- 彩色版 Z 腹部青渐变 `#2fb8a6 → #0e7f8c`，上下横线保持白色
