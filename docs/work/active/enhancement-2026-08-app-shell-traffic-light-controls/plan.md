# macOS 窗口控制视觉优化

## 目标

将自定义标题栏中的关闭、最小化、最大化按钮调整到更接近 Codex/macOS traffic light 的尺寸、亮度和可识别度。

## 范围

- 放大红 / 黄 / 绿圆点，提升深色主题下的对比度。
- 保留现有 Tauri 窗口 API、拖动和权限配置。
- 增加悬停和按下反馈，不改变按钮位置。

## 非目标

- 不改变窗口控制行为、标题栏高度或 Tauri 装饰配置。
