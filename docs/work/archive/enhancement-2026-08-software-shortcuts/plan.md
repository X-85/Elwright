# 常用软件快捷方式

## 目标

在资源工作区增加可启动的软件快捷方式，支持图标化展示和集中管理。

## 范围

- 扩展 workspace resource 为 `app` 类型，保存名称、图标、路径/命令和参数。
- 资源列表显示快捷方式图标；桌面模式点击后按平台启动。
- 浏览器预览保留管理界面，启动操作返回明确降级提示。

## 非目标

- 不扫描系统应用目录，不自动读取应用图标文件。
- 不执行未经用户保存的隐式命令；只启动用户明确配置的路径。

## 验证

- `cargo test`
- `npm test -- --run`
- `npm run build`
