# 应用壳紧凑导航原型（已否决）

> 2026-08-23 用户确认该方向与预期不符，原型代码已恢复到原始应用壳布局。本任务仅保留讨论记录，不进入实现路线。

## 目标

验证恢复顶部一级图标导航、移除固定能力侧栏后，是否能减少顶部割裂感并扩大主工作区。

## 范围

- 顶部栏压缩为品牌、一级视图图标和全局操作。
- 移除固定的左侧能力工具栏。
- 将搜索、筛选、导入和检查更新收回能力工具箱页面顶部。
- 保留右侧上下文面板和终端入口行为。

## 非目标

- 不新增业务功能。
- 不改变能力、AI 对话或消息会话的数据模型。
- 不在本任务中引入代码浏览器入口，待页面结构确认后再接入。

## 验证

- 浏览器预览可正常切换三个一级视图。
- 左栏和右栏显隐不影响主工作区布局。
- 顶部高度减少后，现有能力列表、AI 对话和消息会话不出现溢出或遮挡。
