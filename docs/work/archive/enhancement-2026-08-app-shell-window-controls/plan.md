# 应用壳顶部控制与统一色调

## 目标

修正 Codex 式三栏壳层的顶部控制位置：让终端入口始终可见，并把左栏收起按钮放入左上角的窗口控制组。左栏整栏使用同一面板色，不再让顶部形成更深的独立色块。

## 范围

- 左栏顶部增加统一的窗口控制视觉组和面板操作组。
- 终端入口从默认隐藏的右侧栏迁移到左栏顶部。
- 左侧栏、右侧栏使用一致的面板底色。
- 保留现有终端、设置和左右栏显隐行为。

## 非目标

- 不接管 Tauri 原生窗口的关闭、最小化和最大化行为。
- 不改变终端 IPC、Bridge 或业务面板协议。
