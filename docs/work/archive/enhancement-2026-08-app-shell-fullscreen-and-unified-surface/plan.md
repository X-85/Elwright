# 全屏行为与统一左侧表面

## 目标

让绿色窗口按钮在 macOS 桌面端切换真正的全屏状态，并让应用标题栏与左侧栏使用同一个面板表面色，匹配 Codex 的连续左侧区域。

## 范围

- 使用 Tauri `setFullscreen` / `isFullscreen` 实现绿色按钮切换。
- 增加对应窗口 ACL 权限。
- 将 `.app-chrome` 背景从 `--bg` 改为 `--panel`。

## 非目标

- 不改变普通窗口尺寸、拖动、关闭和最小化逻辑。
- 不改变中间内容区和右侧上下文面板的颜色层级。
