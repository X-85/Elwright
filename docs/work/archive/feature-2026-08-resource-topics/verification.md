# Verification

## 自动化

- `cd src-tauri && cargo test`：通过（39 tests）。
- `cd src-tauri && cargo fmt --check`：通过。
- `cd src && npm test -- --run`：通过（22 tests）。
- `cd src && npm run build`：通过。

## 手动验证清单

- 待在桌面壳中创建三层文件夹并重启应用确认恢复。
- 待添加 URL、本地路径和能力引用后生成课题离线草稿。
