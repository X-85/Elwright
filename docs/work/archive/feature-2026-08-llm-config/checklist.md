# 检查清单

- [x] 配置链（env > config.local.json > ~/.elwright/config.json > $meta.llmDefault）+ 字段级合并
- [x] ew config / config set / config clear（--local 开关）
- [x] 单测 + CLI 手动验证
- [x] llm-setup-guide.md 更新为 config 命令优先
- [ ] 用户确认后归档
