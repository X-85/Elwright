# 变更记录

## 2026-08-31 · 成长体系透明化（ADR-001）

- 新增 `lib/growth.ts` 纯函数：`isUnlocked` / `growthSummary`（锁定清单 + 最近可解锁
  进度）/ `newlyUnlocked`（跨阈值检测）；6 个 vitest。
- 解锁规则三处可视化：列表「待解锁」徽标 tooltip、详情页锁定横幅、侧栏成长提示行，
  统一展示「累计使用任意能力 N 次后自动解锁（当前 T/N）」；缺门槛项显示
  「暂未开放解锁条件」。
- 成长提示：跨过门槛瞬间全局 toast「🎉 已解锁进阶能力「名称」」；侧栏持续显示
  距最近可解锁项的差距。
- 内置 `weekly-report` 升为进阶档位示例（tier 2 + 累计 3 次解锁），成长闭环开箱可用。
- 浏览器预览四层实测通过：提示行进度 → 查看全部出现待解锁徽标 → 详情横幅条件与
  进度 → 第 3 次使用触发解锁 toast 且能力进入核心视图。
- 决策与拒绝项见 `decisions/ADR-001-growth-transparency.md`。

## 未发布

- 增加能力成熟度元数据、核心/全部视图切换、本地使用计数和待解锁提示。
