# 行为

- 收藏夹的主对象是本地文件：用户经系统文件选择器选取文件，再放入最多三层的收藏文件夹；应用只保存文件路径和元数据，不复制、移动或删除原文件。
- 网页链接、能力引用、备注和软件快捷方式属于可选补充资源。软件快捷方式保存图标、应用路径/命令和参数；桌面壳不经 shell 启动，删除只删除快捷方式元数据，不卸载软件或删除用户文件。
- 课题包含标题、问题描述、资源引用和生成的报告。资源可被多个课题引用。
- 报告生成优先使用已配置 LLM；请求失败或未配置时生成离线结构化草稿，并明确标注。
- 所有数据写入用户层 JSON，写入采用临时文件替换，进程重启后恢复。
