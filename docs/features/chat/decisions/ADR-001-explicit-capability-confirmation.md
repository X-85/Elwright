# ADR-001：能力调用必须显式确认

## 状态

已接受（规划决策，待实现验证）

## 决策

AI 对话可以推荐 Elwright 能力并草拟参数，但不能仅凭模型输出直接执行脚本、写文件、调用终端或访问外部系统。展示目标能力、参数和预期影响后，由用户确认再调用现有执行路径。

## 原因

这保持了 Elwright 的可控性和离线优先原则，也避免把不可信的模型文本误当成可执行工具调用。未来若引入更自动化的编排，需要另行设计权限、审计、取消和回滚。
