# 会话问题台账提示词

将下面的提示词粘贴到新会话开始处。它适用于可以读写当前工作区文件的 Agent。

```text
请在本次会话中维护一份轻量级问题台账，帮助我追踪长期会话中的问题、方案和结果。

一、台账文件

如果你可以读写当前工作区，请使用以下文件：

session/
├── index.md
├── events.md
├── decisions.md
└── archive/

如果目录或文件不存在，请创建它们。

文件职责：

- index.md：当前问题索引，只保存问题编号、标题、状态、最近进展和下一步。
- events.md：当前活跃问题的处理过程，每轮追加简短记录。
- decisions.md：最终确认的决定、规则或长期有效结论。
- archive/：已解决问题的完整历史和旧索引。

二、读取策略

1. 每轮开始只读取 session/index.md。
2. 根据当前消息判断属于哪个问题。
3. 只读取 events.md 中与当前问题编号相关的记录。
4. 如果是历史问题，再搜索 session/archive/。
5. 不要每轮读取完整 events.md、decisions.md 或 archive/。
6. 只有我明确要求“总结整个会话”时，才读取全部历史文件。

三、问题编号

1. 每个独立问题分配唯一编号：Q1、Q2、Q3……
2. 针对同一问题的后续追问，继续使用原编号。
3. 只有目标、原因或交付物明显不同，才创建新编号。
4. 如果无法判断是否为同一问题，选择最相关的已有编号，并简要说明理由。

四、问题状态

状态只能使用：

- 待处理
- 处理中
- 已解决
- 阻塞
- 搁置

必须明确区分：

- 已验证
- 未验证
- 失败
- 推测

不能把计划写成结果，也不能把推测写成事实。

五、每轮处理流程

1. 识别当前消息对应的 Q 编号。
2. 正常回答问题或执行任务。
3. 将本轮记录追加到 events.md。
4. 更新 index.md 中该问题的一行摘要。
5. 如果形成了稳定的最终结论，再追加到 decisions.md。
6. 不覆盖或删除已有处理记录。

events.md 中每次只记录：

### Q编号 | 第N次处理
- 问题或新增信息：
- 本轮方案：
- 实际结果：
- 下一步：

记录应保持简短，不要复制整段对话。

六、问题归档

当一个问题已经解决，并且近期不会继续讨论时：

1. 在 events.md 中记录最终结果。
2. 如果有长期有效结论，写入 decisions.md。
3. 将该问题的历史记录移动到 archive/events-YYYY-MM.md。
4. 从当前 events.md 中移除该问题的历史记录。
5. 在 index.md 中保留一行简短结果；长期不再关注的问题可移至 archive/index.md。

decisions.md 只记录最终决定，不替代问题的完整处理历史。

七、定期整理

每处理约 5～10 轮后，进行一次轻量整理：

- 检查活跃问题的状态和下一步。
- 合并明显重复的问题。
- 归档长期已解决的问题。
- 不要为了整理而读取全部历史。
- 不要删除任何有价值的处理记录。

八、回复格式

正常完成回答后，在末尾增加：

[台账更新]
问题：Q编号，或“新建 Q编号”
本轮：一句话总结
状态：当前状态
下一步：一句话总结

如果本轮没有产生新的问题进展，则写：

[台账更新]
无新增问题进展

如果你无法读写工作区文件，必须明确告诉我，不要假装已经更新台账；此时在回复末尾临时维护同样格式的台账。
```

## 文件读取原则

每轮只读取 `session/index.md` 和当前问题的相关记录。不要因为台账存在，就把整个 `events.md` 或归档目录放入上下文。只有在需要回顾全部历史时，才读取全部文件。
